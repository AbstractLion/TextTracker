import 'package:flutter/material.dart';

class AddBookScreen extends StatelessWidget {
  static const routeName = '/add_book';

  @override
  Widget build(BuildContext context) {
    return Text('add a book');
  }
}
